'use client';

// Yorùbá: àkọsílẹ̀ ìkàwé
// This component is now a placeholder - read tracking happens via NEXT button click
export default function ReadTracker({ topicId }: { topicId: string }) {
  return null; // No automatic tracking - only tracks when user clicks NEXT
}
